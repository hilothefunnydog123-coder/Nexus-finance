// AUTO-GENERATED by scripts/build-algos.mjs — do not edit by hand. Re-run the script to refresh.
import type { Algorithm } from './data'

export const QUANT_STRATEGIES: Algorithm[] = [
  {
    "id": "nqtrendpb",
    "instructor": "YN Finance Quant Desk",
    "strategy": "Trend Pullback (Time-Series Momentum)",
    "tagline": "The most robust anomaly in markets: trade WITH the trend. Buy pullbacks to the fast EMA while above the 200, sell the mirror below it. Long/short.",
    "assets": [
      "Futures (NQ/MNQ)",
      "Indices",
      "Stocks"
    ],
    "timeframes": [
      "15m",
      "1H",
      "4H"
    ],
    "propFirms": [
      "Topstep",
      "Apex",
      "MyFundedFutures",
      "FTMO"
    ],
    "winTarget": "~44–48% (2:1) — verify yourself",
    "riskPerTrade": "0.5% risk-based",
    "color": "#00d4ff",
    "init": "TPB",
    "overview": "Time-series momentum is the single most documented, cross-asset, decades-persistent anomaly in the academic literature (Moskowitz, Ooi & Pedersen 2012). This is its simplest honest expression: the 200-EMA defines the regime, and you only take pullbacks in the direction of that trend — a dip to the fast EMA that closes back in the trend direction. One ATR stop, one 2R target, breakeven at +1R. Five inputs. Nothing to overfit. This is a deliberately simple, low-parameter strategy on a well-documented edge — the opposite of an overfit \"AI engine\". It is not magic and makes no hedge-fund claims: validate it in the Strategy Tester with realistic commission + slippage and an out-of-sample/walk-forward split before risking real capital.",
    "propNotes": "Lower win rate, higher R — that is normal and correct for a trend follower; do not \"fix\" it by moving the target in. The 200-EMA regime filter is the whole edge: it keeps you on the side the market is already trending. Works across timeframes and instruments precisely because it is simple. Backtest it; expect a modest, durable edge, not a miracle.",
    "auto": {
      "tradingview": "//@version=5\nstrategy(\"YN Finance — Trend Pullback (Time-Series Momentum) | Robust Edge\", overlay=true, max_boxes_count=200, max_lines_count=200, max_labels_count=200, default_qty_type=strategy.percent_of_equity, default_qty_value=1, commission_type=strategy.commission.percent, commission_value=0.01, slippage=2, process_orders_on_close=true)\n\n// ── Signal (the edge — kept deliberately simple) ──\ntrendLen = input.int(200, \"Trend EMA (regime)\", group=\"Signal\")\npullLen  = input.int(20,  \"Pullback EMA\",        group=\"Signal\")\n\n// ── Risk & exits (one stop, one target, one breakeven) ──\nriskPct = input.float(0.5, \"Risk % of equity per trade\", step=0.1, group=\"Risk & Exits\")\natrLen  = input.int(14,    \"ATR length\",                 group=\"Risk & Exits\")\nslMult  = input.float(1.5, \"Stop (ATR ×)\",     step=0.1, group=\"Risk & Exits\")\ntpR     = input.float(2, \"Target (R)\",  step=0.1, group=\"Risk & Exits\")\nuseBE   = input.bool(true, \"Move to breakeven at +1R\",   group=\"Risk & Exits\")\n\n// ── Session ──\nuseSess = input.bool(true, \"Trade only in session\", group=\"Session\")\nsess    = input.session(\"0930-1600\", \"Session (NY)\", group=\"Session\")\n\ntz = \"America/New_York\"\natr = ta.atr(atrLen)\ninSess = not na(time(timeframe.period, sess, tz))\nsessOk = not useSess or inSess\nbias200 = close > ta.ema(close, 200)\nf_fmt(x) => str.tostring(x, format.mintick)\n\n// ── Strategy core ──\nbool longSig  = false\nbool shortSig = false\nemaT = ta.ema(close, trendLen)\nemaP = ta.ema(close, pullLen)\nupTrend = close > emaT\nslopeUp = emaT > emaT[5]\nlongSig  := upTrend and slopeUp and low <= emaP and close > emaP and close > open\nshortSig := not upTrend and not slopeUp and high >= emaP and close < emaP and close < open\n\n// ── Execution ──\nvar float eEntry = na\nvar float eSL    = na\nvar float eTP    = na\ncanEnter = strategy.position_size == 0 and barstate.isconfirmed and sessOk\n\nif longSig and canEnter\n    eSL := close - atr * slMult\n    rk = close - eSL\n    if rk > 0\n        eEntry := close\n        eTP := close + rk * tpR\n        strategy.entry(\"L\", strategy.long, qty = (strategy.equity * riskPct / 100) / rk)\n        box.new(bar_index, eTP, bar_index + 20, eEntry, border_color=color.new(color.lime, 40), bgcolor=color.new(color.lime, 88))\n        box.new(bar_index, eEntry, bar_index + 20, eSL, border_color=color.new(color.red, 40), bgcolor=color.new(color.red, 88))\n        label.new(bar_index, eTP, \"LONG Trend PB | E \" + f_fmt(eEntry) + \"  TP \" + f_fmt(eTP) + \"  SL \" + f_fmt(eSL), style=label.style_label_down, color=color.new(color.lime, 20), textcolor=color.black, size=size.small)\n        alert(\"TRENDPB LONG \" + syminfo.ticker + \" @ \" + f_fmt(eEntry) + \" | SL \" + f_fmt(eSL) + \" | TP \" + f_fmt(eTP), alert.freq_once_per_bar)\n\nif shortSig and canEnter\n    eSL := close + atr * slMult\n    rk = eSL - close\n    if rk > 0\n        eEntry := close\n        eTP := close - rk * tpR\n        strategy.entry(\"S\", strategy.short, qty = (strategy.equity * riskPct / 100) / rk)\n        box.new(bar_index, eEntry, bar_index + 20, eTP, border_color=color.new(color.lime, 40), bgcolor=color.new(color.lime, 88))\n        box.new(bar_index, eSL, bar_index + 20, eEntry, border_color=color.new(color.red, 40), bgcolor=color.new(color.red, 88))\n        label.new(bar_index, eTP, \"SHORT Trend PB | E \" + f_fmt(eEntry) + \"  TP \" + f_fmt(eTP) + \"  SL \" + f_fmt(eSL), style=label.style_label_up, color=color.new(color.red, 20), textcolor=color.white, size=size.small)\n        alert(\"TRENDPB SHORT \" + syminfo.ticker + \" @ \" + f_fmt(eEntry) + \" | SL \" + f_fmt(eSL) + \" | TP \" + f_fmt(eTP), alert.freq_once_per_bar)\n\nif strategy.position_size > 0\n    rk = eEntry - eSL\n    beStop = useBE and high >= eEntry + rk ? eEntry : eSL\n    strategy.exit(\"L-exit\", from_entry=\"L\", stop=beStop, limit=eTP)\nif strategy.position_size < 0\n    rk = eSL - eEntry\n    beStop = useBE and low <= eEntry - rk ? eEntry : eSL\n    strategy.exit(\"S-exit\", from_entry=\"S\", stop=beStop, limit=eTP)\n\nplot(ta.ema(close, trendLen), \"Trend EMA\", color=color.new(color.orange, 0), linewidth=2)\nplot(ta.ema(close, pullLen),  \"Pullback EMA\", color=color.new(color.aqua, 0))\n\n// ── Minimal dashboard ──\nvar table d = na\nif barstate.islast\n    table.delete(d)\n    d := table.new(position.top_right, 2, 3, border_width=1, frame_color=color.new(color.gray, 50), frame_width=1)\n    posTxt = strategy.position_size > 0 ? \"LONG\" : strategy.position_size < 0 ? \"SHORT\" : \"FLAT\"\n    table.cell(d, 0, 0, \"YN · Trend PB\", text_color=color.white, bgcolor=color.new(#00d4ff, 0), text_size=size.small)\n    table.cell(d, 1, 0, \"ROBUST\", text_color=color.new(color.white, 15), bgcolor=color.new(#00d4ff, 0), text_size=size.small)\n    table.cell(d, 0, 1, \"200-EMA bias\", text_color=color.gray, text_size=size.small)\n    table.cell(d, 1, 1, bias200 ? \"BULL\" : \"BEAR\", text_color = bias200 ? color.lime : color.red, text_size=size.small)\n    table.cell(d, 0, 2, \"Position\", text_color=color.gray, text_size=size.small)\n    table.cell(d, 1, 2, posTxt, text_color = posTxt == \"LONG\" ? color.lime : posTxt == \"SHORT\" ? color.red : color.gray, text_size=size.small)",
      "mt5": "// ── YN Finance — Trend Pullback (Time-Series Momentum) (MetaTrader 5) ──\n// This is a deliberately simple, low-parameter strategy — it ports to MQL5 cleanly:\n//   • ta.ema / ta.rsi / ta.atr  → iMA / iRSI / iATR handles\n//   • session check             → TimeHour/TimeMinute on the server clock\n//   • risk-based sizing          → lots = (equity * risk%) / (stopPoints * tickValue)\n//   • one stop + one target + breakeven → CTrade::PositionOpen + PositionModify\n// A full compilable .mq5 is available on request. The Pine build in the previous tab\n// is the reference. Validate in the Strategy Tester before trading either.",
      "steps": [
        "Open TradingView → Pine Editor → paste the Trend Pullback (Time-Series Momentum) strategy → Add to Chart. Use a 15m/1H/4H chart of Futures (NQ/MNQ).",
        "There are only a handful of inputs on purpose — a simple rule set is far less likely to be curve-fit than a 40-knob \"engine\". Tune the signal lengths and the ATR stop if you must, but resist adding rules.",
        "Risk: \"Risk %\" sizes every trade off the stop distance automatically. One ATR stop, one 2R target, breakeven at +1R — that is the entire exit logic.",
        "Open the Strategy Tester and judge it honestly: net profit AFTER commission + slippage, profit factor, max drawdown, and the trade count. A handful of cherry-picked winners is not an edge.",
        "Walk it forward: optimize on the first 70% of the data, then check the LAST 30% (data it never saw). If it falls apart out-of-sample, it was overfit — trust the out-of-sample number, not the in-sample one.",
        "Expect a modest, durable edge — not a miracle. If it survives realistic costs and out-of-sample testing, THEN consider it for a funded account, at small size first."
      ]
    },
    "signals": {
      "tradingview": "//@version=5\nindicator(\"YN Finance — Trend Pullback (Time-Series Momentum) | Signals\", overlay=true, max_boxes_count=200, max_lines_count=200, max_labels_count=200)\n\n// ── Signal (the edge — kept deliberately simple) ──\ntrendLen = input.int(200, \"Trend EMA (regime)\", group=\"Signal\")\npullLen  = input.int(20,  \"Pullback EMA\",        group=\"Signal\")\natrLen  = input.int(14,    \"ATR length\",          group=\"Risk\")\nslMult  = input.float(1.5, \"Stop (ATR ×)\", step=0.1, group=\"Risk\")\ntpR     = input.float(2, \"Target (R)\", step=0.1, group=\"Risk\")\nuseSess = input.bool(true, \"Only in session\", group=\"Session\")\nsess    = input.session(\"0930-1600\", \"Session (NY)\", group=\"Session\")\nboxBars = input.int(24, \"Trade-box width (bars)\", minval=4, group=\"Display\")\n\ntz = \"America/New_York\"\natr = ta.atr(atrLen)\ninSess = not na(time(timeframe.period, sess, tz))\nsessOk = not useSess or inSess\nbias200 = close > ta.ema(close, 200)\nf_fmt(x) => str.tostring(x, format.mintick)\n\nbool longSig  = false\nbool shortSig = false\nemaT = ta.ema(close, trendLen)\nemaP = ta.ema(close, pullLen)\nupTrend = close > emaT\nslopeUp = emaT > emaT[5]\nlongSig  := upTrend and slopeUp and low <= emaP and close > emaP and close > open\nshortSig := not upTrend and not slopeUp and high >= emaP and close < emaP and close < open\n\nlongOk  = longSig  and sessOk\nshortOk = shortSig and sessOk\n\nif longOk\n    slp = close - atr * slMult\n    tp = close + (close - slp) * tpR\n    box.new(bar_index, tp, bar_index + boxBars, close, border_color=color.new(color.lime, 40), bgcolor=color.new(color.lime, 85))\n    box.new(bar_index, close, bar_index + boxBars, slp, border_color=color.new(color.red, 40), bgcolor=color.new(color.red, 85))\n    label.new(bar_index, tp, \"LONG Trend PB | E \" + f_fmt(close) + \"  TP \" + f_fmt(tp) + \"  SL \" + f_fmt(slp), style=label.style_label_down, color=color.new(color.lime, 20), textcolor=color.black, size=size.small)\n    alert(\"TRENDPB LONG \" + syminfo.ticker + \" | E \" + f_fmt(close) + \" | SL \" + f_fmt(slp) + \" | TP \" + f_fmt(tp), alert.freq_once_per_bar)\n\nif shortOk\n    slp = close + atr * slMult\n    tp = close - (slp - close) * tpR\n    box.new(bar_index, close, bar_index + boxBars, tp, border_color=color.new(color.lime, 40), bgcolor=color.new(color.lime, 85))\n    box.new(bar_index, slp, bar_index + boxBars, close, border_color=color.new(color.red, 40), bgcolor=color.new(color.red, 85))\n    label.new(bar_index, tp, \"SHORT Trend PB | E \" + f_fmt(close) + \"  TP \" + f_fmt(tp) + \"  SL \" + f_fmt(slp), style=label.style_label_up, color=color.new(color.red, 20), textcolor=color.white, size=size.small)\n    alert(\"TRENDPB SHORT \" + syminfo.ticker + \" | E \" + f_fmt(close) + \" | SL \" + f_fmt(slp) + \" | TP \" + f_fmt(tp), alert.freq_once_per_bar)\n\nplot(ta.ema(close, trendLen), \"Trend EMA\", color=color.new(color.orange, 0), linewidth=2)\nplot(ta.ema(close, pullLen),  \"Pullback EMA\", color=color.new(color.aqua, 0))\n\nplotshape(longOk,  title=\"Long\",  style=shape.triangleup,   location=location.belowbar, color=color.lime, size=size.small)\nplotshape(shortOk, title=\"Short\", style=shape.triangledown, location=location.abovebar, color=color.red,  size=size.small)\nalertcondition(longOk,  \"Trend PB Long\",  \"Trend Pullback (Time-Series Momentum) LONG\")\nalertcondition(shortOk, \"Trend PB Short\", \"Trend Pullback (Time-Series Momentum) SHORT\")",
      "steps": [
        "Open TradingView → Pine Editor → paste the Trend Pullback (Time-Series Momentum) signals indicator → Add to Chart (15m/1H/4H chart of Futures (NQ/MNQ)).",
        "On a valid signal the box draws: entry, a green 2R target, a red ATR stop. The label and alert carry the exact prices.",
        "Create an alert → \"Any alert() function call\" → \"Once Per Bar Close\" so you only act on confirmed bars.",
        "This is the manual-execution version of the strategy in the other tab. Backtest the strategy() build first to confirm the edge, then use these signals to trade it by hand."
      ]
    }
  },
  {
    "id": "nqmeanrev",
    "instructor": "YN Finance Quant Desk",
    "strategy": "RSI-2 Mean Reversion (Connors)",
    "tagline": "Buy the deep short-term dip inside an uptrend, sell the rip inside a downtrend. A genuinely persistent retail edge. Long/short.",
    "assets": [
      "Futures (NQ/MNQ)",
      "Indices",
      "Stocks"
    ],
    "timeframes": [
      "5m",
      "15m",
      "1H",
      "Daily"
    ],
    "propFirms": [
      "Topstep",
      "Apex",
      "MyFundedFutures",
      "FTMO"
    ],
    "winTarget": "~60–65% (≈1:1) — verify yourself",
    "riskPerTrade": "0.5% risk-based",
    "color": "#34d399",
    "init": "MR2",
    "overview": "Larry Connors’ RSI-2 is one of the few short-term mean-reversion edges that has held up across decades of public study. The logic: in an uptrend (price above the 200 EMA), a 2-period RSI dropping below the oversold level means price has pulled back hard inside a rising market — the highest-probability dip-buy. Shorts mirror in a downtrend. One ATR stop, a small ≈1R target (reversion is fast), breakeven at +1R. Four signal inputs. This is a deliberately simple, low-parameter strategy on a well-documented edge — the opposite of an overfit \"AI engine\". It is not magic and makes no hedge-fund claims: validate it in the Strategy Tester with realistic commission + slippage and an out-of-sample/walk-forward split before risking real capital.",
    "propNotes": "High win rate, small R — that is the correct shape for mean reversion; never widen the stop chasing a better fill. The 200-EMA regime gate is mandatory — fading WITHOUT the trend is how mean-reversion blows up. Highest-probability on liquid index futures and large-cap stocks. Backtest the oversold/overbought levels; expect a steady, low-variance curve, not fireworks.",
    "auto": {
      "tradingview": "//@version=5\nstrategy(\"YN Finance — RSI-2 Mean Reversion (Connors) | Robust Edge\", overlay=true, max_boxes_count=200, max_lines_count=200, max_labels_count=200, default_qty_type=strategy.percent_of_equity, default_qty_value=1, commission_type=strategy.commission.percent, commission_value=0.01, slippage=2, process_orders_on_close=true)\n\n// ── Signal (the edge — kept deliberately simple) ──\nregimeLen = input.int(200, \"Regime EMA\",        group=\"Signal\")\nrsiLen    = input.int(2,   \"RSI length\",        group=\"Signal\")\nosLevel   = input.float(10, \"Oversold (long)\",  step=1, group=\"Signal\")\nobLevel   = input.float(90, \"Overbought (short)\", step=1, group=\"Signal\")\n\n// ── Risk & exits (one stop, one target, one breakeven) ──\nriskPct = input.float(0.5, \"Risk % of equity per trade\", step=0.1, group=\"Risk & Exits\")\natrLen  = input.int(14,    \"ATR length\",                 group=\"Risk & Exits\")\nslMult  = input.float(1.5, \"Stop (ATR ×)\",     step=0.1, group=\"Risk & Exits\")\ntpR     = input.float(1, \"Target (R)\",  step=0.1, group=\"Risk & Exits\")\nuseBE   = input.bool(true, \"Move to breakeven at +1R\",   group=\"Risk & Exits\")\n\n// ── Session ──\nuseSess = input.bool(true, \"Trade only in session\", group=\"Session\")\nsess    = input.session(\"0930-1600\", \"Session (NY)\", group=\"Session\")\n\ntz = \"America/New_York\"\natr = ta.atr(atrLen)\ninSess = not na(time(timeframe.period, sess, tz))\nsessOk = not useSess or inSess\nbias200 = close > ta.ema(close, 200)\nf_fmt(x) => str.tostring(x, format.mintick)\n\n// ── Strategy core ──\nbool longSig  = false\nbool shortSig = false\nemaR = ta.ema(close, regimeLen)\nr = ta.rsi(close, rsiLen)\nlongSig  := close > emaR and ta.crossunder(r, osLevel)\nshortSig := close < emaR and ta.crossover(r, obLevel)\n\n// ── Execution ──\nvar float eEntry = na\nvar float eSL    = na\nvar float eTP    = na\ncanEnter = strategy.position_size == 0 and barstate.isconfirmed and sessOk\n\nif longSig and canEnter\n    eSL := close - atr * slMult\n    rk = close - eSL\n    if rk > 0\n        eEntry := close\n        eTP := close + rk * tpR\n        strategy.entry(\"L\", strategy.long, qty = (strategy.equity * riskPct / 100) / rk)\n        box.new(bar_index, eTP, bar_index + 20, eEntry, border_color=color.new(color.lime, 40), bgcolor=color.new(color.lime, 88))\n        box.new(bar_index, eEntry, bar_index + 20, eSL, border_color=color.new(color.red, 40), bgcolor=color.new(color.red, 88))\n        label.new(bar_index, eTP, \"LONG RSI-2 MR | E \" + f_fmt(eEntry) + \"  TP \" + f_fmt(eTP) + \"  SL \" + f_fmt(eSL), style=label.style_label_down, color=color.new(color.lime, 20), textcolor=color.black, size=size.small)\n        alert(\"MEANREV LONG \" + syminfo.ticker + \" @ \" + f_fmt(eEntry) + \" | SL \" + f_fmt(eSL) + \" | TP \" + f_fmt(eTP), alert.freq_once_per_bar)\n\nif shortSig and canEnter\n    eSL := close + atr * slMult\n    rk = eSL - close\n    if rk > 0\n        eEntry := close\n        eTP := close - rk * tpR\n        strategy.entry(\"S\", strategy.short, qty = (strategy.equity * riskPct / 100) / rk)\n        box.new(bar_index, eEntry, bar_index + 20, eTP, border_color=color.new(color.lime, 40), bgcolor=color.new(color.lime, 88))\n        box.new(bar_index, eSL, bar_index + 20, eEntry, border_color=color.new(color.red, 40), bgcolor=color.new(color.red, 88))\n        label.new(bar_index, eTP, \"SHORT RSI-2 MR | E \" + f_fmt(eEntry) + \"  TP \" + f_fmt(eTP) + \"  SL \" + f_fmt(eSL), style=label.style_label_up, color=color.new(color.red, 20), textcolor=color.white, size=size.small)\n        alert(\"MEANREV SHORT \" + syminfo.ticker + \" @ \" + f_fmt(eEntry) + \" | SL \" + f_fmt(eSL) + \" | TP \" + f_fmt(eTP), alert.freq_once_per_bar)\n\nif strategy.position_size > 0\n    rk = eEntry - eSL\n    beStop = useBE and high >= eEntry + rk ? eEntry : eSL\n    strategy.exit(\"L-exit\", from_entry=\"L\", stop=beStop, limit=eTP)\nif strategy.position_size < 0\n    rk = eSL - eEntry\n    beStop = useBE and low <= eEntry - rk ? eEntry : eSL\n    strategy.exit(\"S-exit\", from_entry=\"S\", stop=beStop, limit=eTP)\n\nplot(ta.ema(close, regimeLen), \"Regime EMA\", color=color.new(color.orange, 0), linewidth=2)\n\n// ── Minimal dashboard ──\nvar table d = na\nif barstate.islast\n    table.delete(d)\n    d := table.new(position.top_right, 2, 3, border_width=1, frame_color=color.new(color.gray, 50), frame_width=1)\n    posTxt = strategy.position_size > 0 ? \"LONG\" : strategy.position_size < 0 ? \"SHORT\" : \"FLAT\"\n    table.cell(d, 0, 0, \"YN · RSI-2 MR\", text_color=color.white, bgcolor=color.new(#34d399, 0), text_size=size.small)\n    table.cell(d, 1, 0, \"ROBUST\", text_color=color.new(color.white, 15), bgcolor=color.new(#34d399, 0), text_size=size.small)\n    table.cell(d, 0, 1, \"200-EMA bias\", text_color=color.gray, text_size=size.small)\n    table.cell(d, 1, 1, bias200 ? \"BULL\" : \"BEAR\", text_color = bias200 ? color.lime : color.red, text_size=size.small)\n    table.cell(d, 0, 2, \"Position\", text_color=color.gray, text_size=size.small)\n    table.cell(d, 1, 2, posTxt, text_color = posTxt == \"LONG\" ? color.lime : posTxt == \"SHORT\" ? color.red : color.gray, text_size=size.small)",
      "mt5": "// ── YN Finance — RSI-2 Mean Reversion (Connors) (MetaTrader 5) ──\n// This is a deliberately simple, low-parameter strategy — it ports to MQL5 cleanly:\n//   • ta.ema / ta.rsi / ta.atr  → iMA / iRSI / iATR handles\n//   • session check             → TimeHour/TimeMinute on the server clock\n//   • risk-based sizing          → lots = (equity * risk%) / (stopPoints * tickValue)\n//   • one stop + one target + breakeven → CTrade::PositionOpen + PositionModify\n// A full compilable .mq5 is available on request. The Pine build in the previous tab\n// is the reference. Validate in the Strategy Tester before trading either.",
      "steps": [
        "Open TradingView → Pine Editor → paste the RSI-2 Mean Reversion (Connors) strategy → Add to Chart. Use a 5m/15m/1H/Daily chart of Futures (NQ/MNQ).",
        "There are only a handful of inputs on purpose — a simple rule set is far less likely to be curve-fit than a 40-knob \"engine\". Tune the signal lengths and the ATR stop if you must, but resist adding rules.",
        "Risk: \"Risk %\" sizes every trade off the stop distance automatically. One ATR stop, one 1R target, breakeven at +1R — that is the entire exit logic.",
        "Open the Strategy Tester and judge it honestly: net profit AFTER commission + slippage, profit factor, max drawdown, and the trade count. A handful of cherry-picked winners is not an edge.",
        "Walk it forward: optimize on the first 70% of the data, then check the LAST 30% (data it never saw). If it falls apart out-of-sample, it was overfit — trust the out-of-sample number, not the in-sample one.",
        "Expect a modest, durable edge — not a miracle. If it survives realistic costs and out-of-sample testing, THEN consider it for a funded account, at small size first."
      ]
    },
    "signals": {
      "tradingview": "//@version=5\nindicator(\"YN Finance — RSI-2 Mean Reversion (Connors) | Signals\", overlay=true, max_boxes_count=200, max_lines_count=200, max_labels_count=200)\n\n// ── Signal (the edge — kept deliberately simple) ──\nregimeLen = input.int(200, \"Regime EMA\",        group=\"Signal\")\nrsiLen    = input.int(2,   \"RSI length\",        group=\"Signal\")\nosLevel   = input.float(10, \"Oversold (long)\",  step=1, group=\"Signal\")\nobLevel   = input.float(90, \"Overbought (short)\", step=1, group=\"Signal\")\natrLen  = input.int(14,    \"ATR length\",          group=\"Risk\")\nslMult  = input.float(1.5, \"Stop (ATR ×)\", step=0.1, group=\"Risk\")\ntpR     = input.float(1, \"Target (R)\", step=0.1, group=\"Risk\")\nuseSess = input.bool(true, \"Only in session\", group=\"Session\")\nsess    = input.session(\"0930-1600\", \"Session (NY)\", group=\"Session\")\nboxBars = input.int(24, \"Trade-box width (bars)\", minval=4, group=\"Display\")\n\ntz = \"America/New_York\"\natr = ta.atr(atrLen)\ninSess = not na(time(timeframe.period, sess, tz))\nsessOk = not useSess or inSess\nbias200 = close > ta.ema(close, 200)\nf_fmt(x) => str.tostring(x, format.mintick)\n\nbool longSig  = false\nbool shortSig = false\nemaR = ta.ema(close, regimeLen)\nr = ta.rsi(close, rsiLen)\nlongSig  := close > emaR and ta.crossunder(r, osLevel)\nshortSig := close < emaR and ta.crossover(r, obLevel)\n\nlongOk  = longSig  and sessOk\nshortOk = shortSig and sessOk\n\nif longOk\n    slp = close - atr * slMult\n    tp = close + (close - slp) * tpR\n    box.new(bar_index, tp, bar_index + boxBars, close, border_color=color.new(color.lime, 40), bgcolor=color.new(color.lime, 85))\n    box.new(bar_index, close, bar_index + boxBars, slp, border_color=color.new(color.red, 40), bgcolor=color.new(color.red, 85))\n    label.new(bar_index, tp, \"LONG RSI-2 MR | E \" + f_fmt(close) + \"  TP \" + f_fmt(tp) + \"  SL \" + f_fmt(slp), style=label.style_label_down, color=color.new(color.lime, 20), textcolor=color.black, size=size.small)\n    alert(\"MEANREV LONG \" + syminfo.ticker + \" | E \" + f_fmt(close) + \" | SL \" + f_fmt(slp) + \" | TP \" + f_fmt(tp), alert.freq_once_per_bar)\n\nif shortOk\n    slp = close + atr * slMult\n    tp = close - (slp - close) * tpR\n    box.new(bar_index, close, bar_index + boxBars, tp, border_color=color.new(color.lime, 40), bgcolor=color.new(color.lime, 85))\n    box.new(bar_index, slp, bar_index + boxBars, close, border_color=color.new(color.red, 40), bgcolor=color.new(color.red, 85))\n    label.new(bar_index, tp, \"SHORT RSI-2 MR | E \" + f_fmt(close) + \"  TP \" + f_fmt(tp) + \"  SL \" + f_fmt(slp), style=label.style_label_up, color=color.new(color.red, 20), textcolor=color.white, size=size.small)\n    alert(\"MEANREV SHORT \" + syminfo.ticker + \" | E \" + f_fmt(close) + \" | SL \" + f_fmt(slp) + \" | TP \" + f_fmt(tp), alert.freq_once_per_bar)\n\nplot(ta.ema(close, regimeLen), \"Regime EMA\", color=color.new(color.orange, 0), linewidth=2)\n\nplotshape(longOk,  title=\"Long\",  style=shape.triangleup,   location=location.belowbar, color=color.lime, size=size.small)\nplotshape(shortOk, title=\"Short\", style=shape.triangledown, location=location.abovebar, color=color.red,  size=size.small)\nalertcondition(longOk,  \"RSI-2 MR Long\",  \"RSI-2 Mean Reversion (Connors) LONG\")\nalertcondition(shortOk, \"RSI-2 MR Short\", \"RSI-2 Mean Reversion (Connors) SHORT\")",
      "steps": [
        "Open TradingView → Pine Editor → paste the RSI-2 Mean Reversion (Connors) signals indicator → Add to Chart (5m/15m/1H/Daily chart of Futures (NQ/MNQ)).",
        "On a valid signal the box draws: entry, a green 1R target, a red ATR stop. The label and alert carry the exact prices.",
        "Create an alert → \"Any alert() function call\" → \"Once Per Bar Close\" so you only act on confirmed bars.",
        "This is the manual-execution version of the strategy in the other tab. Backtest the strategy() build first to confirm the edge, then use these signals to trade it by hand."
      ]
    }
  },
  {
    "id": "nqorb",
    "instructor": "YN Finance Quant Desk",
    "strategy": "Opening-Range Breakout",
    "tagline": "The first 15 minutes set the day’s range. Trade the break of it. A simple, well-known session-momentum edge. Long/short.",
    "assets": [
      "Futures (NQ/MNQ)",
      "Indices"
    ],
    "timeframes": [
      "1m",
      "5m"
    ],
    "propFirms": [
      "Topstep",
      "Apex",
      "MyFundedFutures"
    ],
    "winTarget": "~40–45% (2:1) — verify yourself",
    "riskPerTrade": "0.5% risk-based",
    "color": "#f59e0b",
    "init": "ORB",
    "overview": "The opening-range breakout is one of the oldest, simplest intraday edges: the first 15 minutes of the cash session (09:30–09:45 ET) build a range that reflects the overnight battle; a clean break of that range in the first couple of hours tends to carry on real participation. Break of the OR high goes long, break of the low goes short, one trade per side. One ATR stop, one 2R target, breakeven at +1R. Two signal inputs (the two session windows). This is a deliberately simple, low-parameter strategy on a well-documented edge — the opposite of an overfit \"AI engine\". It is not magic and makes no hedge-fund claims: validate it in the Strategy Tester with realistic commission + slippage and an out-of-sample/walk-forward split before risking real capital.",
    "propNotes": "Low win rate, high R — the days it trends pay for the days it chops, so do not move the target in. Best on the index futures (NQ/ES) where the open is liquid and directional. A wide opening range = wider stop = smaller size automatically. Skip it on flat, rangebound mornings. Backtest the OR length and the trade window.",
    "auto": {
      "tradingview": "//@version=5\nstrategy(\"YN Finance — Opening-Range Breakout | Robust Edge\", overlay=true, max_boxes_count=200, max_lines_count=200, max_labels_count=200, default_qty_type=strategy.percent_of_equity, default_qty_value=1, commission_type=strategy.commission.percent, commission_value=0.01, slippage=2, process_orders_on_close=true)\n\n// ── Signal (the edge — kept deliberately simple) ──\norSess = input.session(\"0930-0945\", \"Opening range (NY)\", group=\"Signal\")\ntrSess = input.session(\"0945-1130\", \"Trade window (NY)\",  group=\"Signal\")\n\n// ── Risk & exits (one stop, one target, one breakeven) ──\nriskPct = input.float(0.5, \"Risk % of equity per trade\", step=0.1, group=\"Risk & Exits\")\natrLen  = input.int(14,    \"ATR length\",                 group=\"Risk & Exits\")\nslMult  = input.float(1.5, \"Stop (ATR ×)\",     step=0.1, group=\"Risk & Exits\")\ntpR     = input.float(2, \"Target (R)\",  step=0.1, group=\"Risk & Exits\")\nuseBE   = input.bool(true, \"Move to breakeven at +1R\",   group=\"Risk & Exits\")\n\n// ── Session ──\nuseSess = input.bool(false, \"Trade only in session\", group=\"Session\")\nsess    = input.session(\"0930-1600\", \"Session (NY)\", group=\"Session\")\n\ntz = \"America/New_York\"\natr = ta.atr(atrLen)\ninSess = not na(time(timeframe.period, sess, tz))\nsessOk = not useSess or inSess\nbias200 = close > ta.ema(close, 200)\nf_fmt(x) => str.tostring(x, format.mintick)\n\n// ── Strategy core ──\nbool longSig  = false\nbool shortSig = false\ninOR = not na(time(timeframe.period, orSess, tz))\ninTR = not na(time(timeframe.period, trSess, tz))\nnewOR = inOR and not inOR[1]\nvar float orH = na\nvar float orL = na\nvar bool lDone = false\nvar bool sDone = false\nif newOR\n    orH := high\n    orL := low\n    lDone := false\n    sDone := false\nelse if inOR\n    orH := math.max(orH, high)\n    orL := math.min(orL, low)\nvolOk = volume > ta.sma(volume, 20)\nlongSig  := inTR and not lDone and high > orH and close > orH and volOk\nshortSig := inTR and not sDone and low  < orL and close < orL and volOk\nif longSig\n    lDone := true\nif shortSig\n    sDone := true\n\n// ── Execution ──\nvar float eEntry = na\nvar float eSL    = na\nvar float eTP    = na\ncanEnter = strategy.position_size == 0 and barstate.isconfirmed and sessOk\n\nif longSig and canEnter\n    eSL := close - atr * slMult\n    rk = close - eSL\n    if rk > 0\n        eEntry := close\n        eTP := close + rk * tpR\n        strategy.entry(\"L\", strategy.long, qty = (strategy.equity * riskPct / 100) / rk)\n        box.new(bar_index, eTP, bar_index + 20, eEntry, border_color=color.new(color.lime, 40), bgcolor=color.new(color.lime, 88))\n        box.new(bar_index, eEntry, bar_index + 20, eSL, border_color=color.new(color.red, 40), bgcolor=color.new(color.red, 88))\n        label.new(bar_index, eTP, \"LONG OR Break | E \" + f_fmt(eEntry) + \"  TP \" + f_fmt(eTP) + \"  SL \" + f_fmt(eSL), style=label.style_label_down, color=color.new(color.lime, 20), textcolor=color.black, size=size.small)\n        alert(\"ORB LONG \" + syminfo.ticker + \" @ \" + f_fmt(eEntry) + \" | SL \" + f_fmt(eSL) + \" | TP \" + f_fmt(eTP), alert.freq_once_per_bar)\n\nif shortSig and canEnter\n    eSL := close + atr * slMult\n    rk = eSL - close\n    if rk > 0\n        eEntry := close\n        eTP := close - rk * tpR\n        strategy.entry(\"S\", strategy.short, qty = (strategy.equity * riskPct / 100) / rk)\n        box.new(bar_index, eEntry, bar_index + 20, eTP, border_color=color.new(color.lime, 40), bgcolor=color.new(color.lime, 88))\n        box.new(bar_index, eSL, bar_index + 20, eEntry, border_color=color.new(color.red, 40), bgcolor=color.new(color.red, 88))\n        label.new(bar_index, eTP, \"SHORT OR Break | E \" + f_fmt(eEntry) + \"  TP \" + f_fmt(eTP) + \"  SL \" + f_fmt(eSL), style=label.style_label_up, color=color.new(color.red, 20), textcolor=color.white, size=size.small)\n        alert(\"ORB SHORT \" + syminfo.ticker + \" @ \" + f_fmt(eEntry) + \" | SL \" + f_fmt(eSL) + \" | TP \" + f_fmt(eTP), alert.freq_once_per_bar)\n\nif strategy.position_size > 0\n    rk = eEntry - eSL\n    beStop = useBE and high >= eEntry + rk ? eEntry : eSL\n    strategy.exit(\"L-exit\", from_entry=\"L\", stop=beStop, limit=eTP)\nif strategy.position_size < 0\n    rk = eSL - eEntry\n    beStop = useBE and low <= eEntry - rk ? eEntry : eSL\n    strategy.exit(\"S-exit\", from_entry=\"S\", stop=beStop, limit=eTP)\n\nplot(not na(orH) ? orH : na, \"OR High\", color=color.new(color.lime, 40), style=plot.style_linebr)\nplot(not na(orL) ? orL : na, \"OR Low\",  color=color.new(color.red, 40), style=plot.style_linebr)\n\n// ── Minimal dashboard ──\nvar table d = na\nif barstate.islast\n    table.delete(d)\n    d := table.new(position.top_right, 2, 3, border_width=1, frame_color=color.new(color.gray, 50), frame_width=1)\n    posTxt = strategy.position_size > 0 ? \"LONG\" : strategy.position_size < 0 ? \"SHORT\" : \"FLAT\"\n    table.cell(d, 0, 0, \"YN · OR Break\", text_color=color.white, bgcolor=color.new(#f59e0b, 0), text_size=size.small)\n    table.cell(d, 1, 0, \"ROBUST\", text_color=color.new(color.white, 15), bgcolor=color.new(#f59e0b, 0), text_size=size.small)\n    table.cell(d, 0, 1, \"200-EMA bias\", text_color=color.gray, text_size=size.small)\n    table.cell(d, 1, 1, bias200 ? \"BULL\" : \"BEAR\", text_color = bias200 ? color.lime : color.red, text_size=size.small)\n    table.cell(d, 0, 2, \"Position\", text_color=color.gray, text_size=size.small)\n    table.cell(d, 1, 2, posTxt, text_color = posTxt == \"LONG\" ? color.lime : posTxt == \"SHORT\" ? color.red : color.gray, text_size=size.small)",
      "mt5": "// ── YN Finance — Opening-Range Breakout (MetaTrader 5) ──\n// This is a deliberately simple, low-parameter strategy — it ports to MQL5 cleanly:\n//   • ta.ema / ta.rsi / ta.atr  → iMA / iRSI / iATR handles\n//   • session check             → TimeHour/TimeMinute on the server clock\n//   • risk-based sizing          → lots = (equity * risk%) / (stopPoints * tickValue)\n//   • one stop + one target + breakeven → CTrade::PositionOpen + PositionModify\n// A full compilable .mq5 is available on request. The Pine build in the previous tab\n// is the reference. Validate in the Strategy Tester before trading either.",
      "steps": [
        "Open TradingView → Pine Editor → paste the Opening-Range Breakout strategy → Add to Chart. Use a 1m/5m chart of Futures (NQ/MNQ).",
        "There are only a handful of inputs on purpose — a simple rule set is far less likely to be curve-fit than a 40-knob \"engine\". Tune the signal lengths and the ATR stop if you must, but resist adding rules.",
        "Risk: \"Risk %\" sizes every trade off the stop distance automatically. One ATR stop, one 2R target, breakeven at +1R — that is the entire exit logic.",
        "Open the Strategy Tester and judge it honestly: net profit AFTER commission + slippage, profit factor, max drawdown, and the trade count. A handful of cherry-picked winners is not an edge.",
        "Walk it forward: optimize on the first 70% of the data, then check the LAST 30% (data it never saw). If it falls apart out-of-sample, it was overfit — trust the out-of-sample number, not the in-sample one.",
        "Expect a modest, durable edge — not a miracle. If it survives realistic costs and out-of-sample testing, THEN consider it for a funded account, at small size first."
      ]
    },
    "signals": {
      "tradingview": "//@version=5\nindicator(\"YN Finance — Opening-Range Breakout | Signals\", overlay=true, max_boxes_count=200, max_lines_count=200, max_labels_count=200)\n\n// ── Signal (the edge — kept deliberately simple) ──\norSess = input.session(\"0930-0945\", \"Opening range (NY)\", group=\"Signal\")\ntrSess = input.session(\"0945-1130\", \"Trade window (NY)\",  group=\"Signal\")\natrLen  = input.int(14,    \"ATR length\",          group=\"Risk\")\nslMult  = input.float(1.5, \"Stop (ATR ×)\", step=0.1, group=\"Risk\")\ntpR     = input.float(2, \"Target (R)\", step=0.1, group=\"Risk\")\nuseSess = input.bool(false, \"Only in session\", group=\"Session\")\nsess    = input.session(\"0930-1600\", \"Session (NY)\", group=\"Session\")\nboxBars = input.int(24, \"Trade-box width (bars)\", minval=4, group=\"Display\")\n\ntz = \"America/New_York\"\natr = ta.atr(atrLen)\ninSess = not na(time(timeframe.period, sess, tz))\nsessOk = not useSess or inSess\nbias200 = close > ta.ema(close, 200)\nf_fmt(x) => str.tostring(x, format.mintick)\n\nbool longSig  = false\nbool shortSig = false\ninOR = not na(time(timeframe.period, orSess, tz))\ninTR = not na(time(timeframe.period, trSess, tz))\nnewOR = inOR and not inOR[1]\nvar float orH = na\nvar float orL = na\nvar bool lDone = false\nvar bool sDone = false\nif newOR\n    orH := high\n    orL := low\n    lDone := false\n    sDone := false\nelse if inOR\n    orH := math.max(orH, high)\n    orL := math.min(orL, low)\nvolOk = volume > ta.sma(volume, 20)\nlongSig  := inTR and not lDone and high > orH and close > orH and volOk\nshortSig := inTR and not sDone and low  < orL and close < orL and volOk\nif longSig\n    lDone := true\nif shortSig\n    sDone := true\n\nlongOk  = longSig  and sessOk\nshortOk = shortSig and sessOk\n\nif longOk\n    slp = close - atr * slMult\n    tp = close + (close - slp) * tpR\n    box.new(bar_index, tp, bar_index + boxBars, close, border_color=color.new(color.lime, 40), bgcolor=color.new(color.lime, 85))\n    box.new(bar_index, close, bar_index + boxBars, slp, border_color=color.new(color.red, 40), bgcolor=color.new(color.red, 85))\n    label.new(bar_index, tp, \"LONG OR Break | E \" + f_fmt(close) + \"  TP \" + f_fmt(tp) + \"  SL \" + f_fmt(slp), style=label.style_label_down, color=color.new(color.lime, 20), textcolor=color.black, size=size.small)\n    alert(\"ORB LONG \" + syminfo.ticker + \" | E \" + f_fmt(close) + \" | SL \" + f_fmt(slp) + \" | TP \" + f_fmt(tp), alert.freq_once_per_bar)\n\nif shortOk\n    slp = close + atr * slMult\n    tp = close - (slp - close) * tpR\n    box.new(bar_index, close, bar_index + boxBars, tp, border_color=color.new(color.lime, 40), bgcolor=color.new(color.lime, 85))\n    box.new(bar_index, slp, bar_index + boxBars, close, border_color=color.new(color.red, 40), bgcolor=color.new(color.red, 85))\n    label.new(bar_index, tp, \"SHORT OR Break | E \" + f_fmt(close) + \"  TP \" + f_fmt(tp) + \"  SL \" + f_fmt(slp), style=label.style_label_up, color=color.new(color.red, 20), textcolor=color.white, size=size.small)\n    alert(\"ORB SHORT \" + syminfo.ticker + \" | E \" + f_fmt(close) + \" | SL \" + f_fmt(slp) + \" | TP \" + f_fmt(tp), alert.freq_once_per_bar)\n\nplot(not na(orH) ? orH : na, \"OR High\", color=color.new(color.lime, 40), style=plot.style_linebr)\nplot(not na(orL) ? orL : na, \"OR Low\",  color=color.new(color.red, 40), style=plot.style_linebr)\n\nplotshape(longOk,  title=\"Long\",  style=shape.triangleup,   location=location.belowbar, color=color.lime, size=size.small)\nplotshape(shortOk, title=\"Short\", style=shape.triangledown, location=location.abovebar, color=color.red,  size=size.small)\nalertcondition(longOk,  \"OR Break Long\",  \"Opening-Range Breakout LONG\")\nalertcondition(shortOk, \"OR Break Short\", \"Opening-Range Breakout SHORT\")",
      "steps": [
        "Open TradingView → Pine Editor → paste the Opening-Range Breakout signals indicator → Add to Chart (1m/5m chart of Futures (NQ/MNQ)).",
        "On a valid signal the box draws: entry, a green 2R target, a red ATR stop. The label and alert carry the exact prices.",
        "Create an alert → \"Any alert() function call\" → \"Once Per Bar Close\" so you only act on confirmed bars.",
        "This is the manual-execution version of the strategy in the other tab. Backtest the strategy() build first to confirm the edge, then use these signals to trade it by hand."
      ]
    }
  }
]
